# CareerLM environment variable template
# Sprint 1 currently requires no external service credentials.
# Never commit real API keys, passwords, tokens, or secrets.

# Optional path for local CareerLM persistence.
# If omitted, CareerLM stores development data in .careerlm/data.json.
CAREERLM_DATA_FILE=